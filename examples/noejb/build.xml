<?xml version="1.0"?>

<!-- ======================================================================= -->
<!-- JBoss build file                                                       -->
<!-- ======================================================================= -->

<project name="JBoss" default="deploy" basedir=".">

   <property environment="env"/>
   <property name="src.dir" value="${basedir}/src"/>
   <property name="resources.dir" value="${basedir}/resources"/>
   <property name="jboss.home" value="${env.JBOSS_HOME}"/>
   <property name="output.dir" value="${basedir}/output"/>
   <property name="deploy.dir" value="${jboss.home}/server/default/deploy"/>
   <property name="output.classes.dir" value="${output.dir}/classes"/>
   <property name="output.war.dir" value="${output.dir}/war"/>
   <property name="example.name" value="seam-noejb"/>

   <!-- Build classpath -->
   <path id="classpath">
      <fileset dir="${basedir}/../../output">
         <include name="jboss-seam.jar"/>
      </fileset>
      <fileset dir="${basedir}/../../embedded-ejb/lib">
         <include name="*.jar" />
      </fileset>
      <fileset dir="${jboss.home}/server/default/deploy/jbossweb-tomcat55.sar/jsf-libs">
         <include name="**/*.jar"/>
      </fileset>
      <fileset dir="${jboss.home}/lib">
         <include name="**/*.jar"/>
      </fileset>
      <fileset dir="${jboss.home}/server/default/lib">
         <include name="**/*.jar"/>
      </fileset>
      <fileset dir="${jboss.home}/server/default/deploy/jboss-aop-jdk50.deployer">
         <include name="*.jar"/>
      </fileset>
    <fileset dir="${basedir}/../../lib">
       <include name="testng*.jar"/>
    </fileset>
   </path>

   <property name="build.classpath" refid="classpath"/>

   <!-- =================================================================== -->
   <!-- Prepares the build directory                                        -->
   <!-- =================================================================== -->
   <target name="prepare">
      <mkdir dir="${output.dir}"/>
      <mkdir dir="${output.classes.dir}"/>
      <mkdir dir="${output.war.dir}"/>
   </target>

   <!-- =================================================================== -->
   <!-- Compiles the source code                                            -->
   <!-- =================================================================== -->
   <target name="compile" depends="prepare">
      <javac srcdir="${src.dir}"
         destdir="${output.classes.dir}"
         debug="on"
         deprecation="on"
         optimize="off"
         includes="**"
      	 excludes="**/test/*.java">
         <classpath refid="classpath"/>
      </javac>
    </target>
       
   <target name="war" depends="jar,compile">
     <jar destfile="${output.dir}/${example.name}.war">
       <zipfileset dir="${resources.dir}/WEB-INF" prefix="WEB-INF"/>
       <zipfileset dir="${output.dir}" prefix="WEB-INF/lib">
         <include name="${example.name}.jar"/>
       </zipfileset>
       <zipfileset dir="${basedir}/../../output" prefix="WEB-INF/lib">
         <include name="jboss-seam.jar"/>
       </zipfileset>
       <zipfileset dir="${resources.dir}/view"/>
     </jar>
   </target>
    
   <target name="jar" depends="compile">
      <jar jarfile="${output.dir}/${example.name}.jar">
        <fileset dir="${output.classes.dir}">
          <include name="**/*.class"/>
        </fileset>
        <fileset dir="${resources.dir}">
          <include name="import.sql"/>
          <include name="hibernate.cfg.xml"/>
          <include name="seam.properties"/>
        </fileset>
      </jar>
   </target>

   <target name="deploy" depends="war">
   	   <copy file="${resources.dir}/noejb-ds.xml" todir="${deploy.dir}"/>
       <copy file="${output.dir}/${example.name}.war" todir="${deploy.dir}"/>
   </target>

   <!-- =================================================================== -->
   <!-- Cleans up generated stuff                                           -->
   <!-- =================================================================== -->
   <target name="clean.db">
      <delete dir="${deploy.dir}/../data/hypersonic"/>
   </target>

   <target name="clean">
      <delete dir="${output.dir}"/>
      <delete file="${deploy.dir}/${example.name}.war"/>
   </target>

</project>

