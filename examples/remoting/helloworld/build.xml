<?xml version="1.0"?>

<project name="HelloWorld" default="deploy" basedir=".">

    <!-- Example name -->
    <property name="Name"                   value="Seam Remoting HelloWorld Example"/>
    <property name="example.name"           value="seam-remoting-helloworld"/>

	<fileset id="web.xml.file"
	       file="resources/WEB-INF/web.xml"/>

    <!-- WAR -->
    <zipfileset id="example.war.docroot"
               dir="view">
      <include name="**/*"/>
    </zipfileset>
    <zipfileset id="example.war.webinf"
            prefix="WEB-INF"
               dir="resources/WEB-INF" >
        <patternset refid="meta.files"/>
     </zipfileset>
    <zipfileset id="example.war.webinf.lib"
            prefix="WEB-INF/lib"
               dir="../../../facelets/lib">
        <include name="*.jar"/>
    </zipfileset>
    <zipfileset id="example.war.webinf.lib.extra" 
            prefix="WEB-INF/lib"
               dir=".">
        <include name="_NONE_"/>
    </zipfileset>

    <!-- EJB3 -->
    <fileset id="example.ejb3.root"
            dir="resources">
        <include name="seam.properties"/>
    </fileset>

    <!-- EAR -->
    <zipfileset id="example.ear.resources"
            dir="resources">
    	<!--include name="pageflow.jpdl.xml"/>
    	<include name="jbpm.cfg.xml"/-->
      <include name="META-INF/*"/>
    </zipfileset>

    <fileset id="example.ejb3.lib" dir="../../../lib">
      <include name="_NONE_"/>
    </fileset>
  
	<!-- Test -->
	<fileset id="example.resources" dir="resources">
		<include name="**/*.*"/>
	</fileset>

    <!-- Deploy -->
    <fileset id="example.deploy"
            dir="resources">
        <include name="_NONE_"/>
    </fileset>

    <!-- Undeploy -->
    <patternset id="example.undeploy">
        <include name="_NONE_"/>
    </patternset>

    <!-- Overrides -->
    <property name="src.java.dir" value="src"/>
	<property name="src.test.dir" value="src"/>
	<property name="test.classpath" value="test.eejb.classpath"/>

    <import file="../../../build.xml"/>

</project>

